DATABASE_URL="file:./dev.db"

# DeepSeek (OpenAI-compatible) — leave DEEPSEEK_API_KEY empty to use the
# built-in mock interpreter. Set it to a real key to switch to live DeepSeek.
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL="https://api.deepseek.com"
